rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to read and write to any path.
    // This is a temporary broad rule to ensure functionality.
    // We will tighten this up later.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
