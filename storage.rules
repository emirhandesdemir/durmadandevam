
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access for everyone and write access for any authenticated user.
    // This is a permissive rule set for development and testing.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
