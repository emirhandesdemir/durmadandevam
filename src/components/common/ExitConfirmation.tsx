// This component has been removed as it uses browser history manipulation
// that can cause instability, especially on Safari.
// The file is kept to avoid breaking imports but its functionality is disabled.
export default function ExitConfirmation() {
  return null;
}
