// THIS FILE IS NOW DEPRECATED as image storage has been moved to Cloudinary.
// You can remove these rules from your Firebase console.

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All paths are now closed by default
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
